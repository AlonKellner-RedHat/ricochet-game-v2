# GAME_SPEC.md — Revision History

| Version | Notes |
|---------|-------|
| 1.0.0 | Unified game specification consolidating FIRST_PRINCIPLES.md, DESIGN.md, and ARCHITECTURE.md. Added game loop, target mechanics, player physics, level editing, UX, visual language, implementation phasing, and testing strategy. |
| 1.1.0 | Segment simplified to three points (start, end, via). Carrier is derived, not stored. Carrier ↔ via derivation cached bidirectionally for exact round-trip. |
| 1.2.0 | Core principles extended from 20 to 25: derive don't duplicate, exact reversal, designer intent over arithmetic, compute in simplest frame, simulation vs presentation separation. |
| 1.3.0 | Multi-shot puzzles, no-failure state, automatic checkpoints, undo-last-shot. Right-click on empty space fires. Clear plan without reset. Duplicate surfaces in plan allowed (bypassed if unreachable). Semi-circle projection always normal to diameter. §13.1 generalized for Möbius. Step system rewritten: two trace modes (PLANNED/PHYSICAL), five step types (ALIGNED, ALIGNED_POST_PLANNED, DIVERGED_PHYSICAL, DIVERGED_PLANNED, DIVERGED_POST_PLANNED), exact divergence definition. Visibility radial ordering by ray obstruction side provenance. |
| 1.4.0 | Review fixes: 3 contradictions resolved (§24.3 multi-shot win, §19.3 serialization stores cache, §13.4 side+state params). 11 gaps filled (plan selects surface sides, worked examples §16, arrow origin, click tolerance, ray escape, StepTree struct, visibility pseudocode, player-arrow non-collision, gravity/speed defaults, effect Möbius matrices §10.8, checkpoint contents). 5 clarity fixes (INF representation, semi-circle normal vs side, pseudocode note, collinearity by provenance, Phase 1 carrier). 6 minor fixes (right-click semantics, state_key note, redundant definitions, transform cost, editor UX, test levels). Structural: data flow diagram §20.3, open design decisions §33. |
| 1.5.0 | Second review fixes: subsection numbering corrected (§17–§31). Anti-conformal composition specified (conjugating flag, 4 composition cases, §5.2). Planned trace → Step conversion (§13.9). Side determination formula (§9.2). State simulation during planning (§13.8). Arc containment test (§11.1). Right-click contradiction fixed (§3.4). Post-fire plan retained. Initial game state flags in LevelData. Target+effect interaction clarified. CompoundTransformativeEffect is subtype. Godot time-freeze mechanism. Transform IDs. Bypass per-frame. Z-order. Editor scope. Forward-reference note. Two new worked examples (§16.5 bypass, §16.6 state change). Performance target 5ms. Arrow visual specs. |
| 1.6.0 | Third review fixes: circle inversion matrix corrected (α=center, β=r²-\|center\|², γ=1, δ=-conj(center)). Hit limit unified to 256 for all hits (including pass-through). Terminal effects: interactive flag on SideConfig, configurable per side. trace() explicit copy semantics. Bypass is part of planning (data flow fixed). Carrier-type validation in constraints. Pass-through exclusion tracking. plan_transformative_subchain signature fixed. Visibility rewritten as plan-aware light-cone propagation (§15). player_solid flag on Surface. Gravity/movement clarified. Cache multi-key support. Zero-length Direction handled. GUT testing framework. Visual state changes during flight. Ray escape behavior. Editor via drag. Error handling policy (§31.8). GDScript perf note. Phase 9 split. Resource mapping. Reading guide. Sub-chain in glossary. |
| 1.7.0 | Fourth review fixes: renamed midpoint→via globally. Fire no-op on zero-direction. Bypass per-frame in glossary. Hit limit unified to 256 for all hits. CheckpointData struct with targets_hit. Surface side coloring. Animation speed 800u/s. Arrow disappears after shot. Empty plan validity. Semi-circle projection redesigned (normal direction defines diameter). Editor surface IDs monotonic. Cross-surface state changes. Level bounds manual. Camera follows arrow. Step type Z-order. Post-cursor continuation clarified. Plan references by surface ID. Direction value type. Frame transform convention. Initial screen state. Phase dependencies. Arrow size increased. |
| 1.8.0 | Fifth review fixes: terminal-in-planner fixed (closest-point back-propagation, post-terminal entries bypassed). Two-pass planning pseudocode (backward geometry + forward origin fill). Unbounded carrier intersection (§11.5). State-conditional model redesigned (ConfigResolver abstraction: FixedResolver, CategoricalResolver). Bypass vs invalidity contradiction resolved (null back-prop = bypass). Version history moved to CHANGELOG.md. Post-cursor continuation frame clarified. Frame identity by transform ID. Empty-plan step tree defined. Surface naming. Player collision shapes. Jump mechanic. Target hit registration. Target indicator. Animation skip. Step tree merge by index. Complex number GDScript helpers. Provenance ID bootstrapping. Line width defaults. Composition convention note. |
| 1.9.0 | Sixth review fixes: per-entry state snapshots in planner. Merge algorithm rewritten as explicit index-based state machine. Arc-aware visibility guidance with truncating-boundary sub-segment propagation. ConfigResolver missing-key behavior. targets_hit in trace pseudocode. Level bounds in LevelData. Back-propagation segment bounds. Post-cursor continuation clarified (second physical trace in planned frame). Camera defaults (Godot smoothing). Shot lifecycle walkthrough (§16.9). State ownership table (§18.4). §16.6 uses CategoricalResolver. |
| 2.0.0 | Seventh review fixes: step tree merge post-cursor contradiction resolved (planned_steps = pre+post cursor concatenation, empty-plan guard). Godot pinned to 4.6+. Systematic angle→cross-product replacement (arc containment, click detection, arrow animation, visibility). draw_arc() clockwise conversion. Phase 0 (core abstractions) added. Rigid motion δ→d rename. Terminal frame behavior clarified. Initial Direction construction. Player collision CircleShape2D r=12. Jump params (400u/s, single, no coyote). player_solid scope clarified. Audio placeholders. Save format ConfigFile. INF guard in visual conversion. Executive summary. |
| 2.2.0 | Invariant testing expanded: 30 rigorously testable invariants in two tiers (11 user-experience, 19 systemic). Replaces the previous 6-item flat list. Each invariant references the spec section it derives from and is dynamically checkable at runtime. |
| 2.1.0 | Eighth review fixes: W key context-sensitive (jump with gravity, move-up without). Cursor world-space. TracedPath type defined. Terminal planner output uses surface projection. Pass-through exclusion upgraded to Set. Visibility algorithm fully specified as plan-aware loop reusing physical trace casting code. Game state timing fixed (promote before unfreeze). Merge uses provenance IDs. Frame ID as invariant. targets_hit union semantics. Arc collision uses ConcavePolygonShape2D. Level discovery by directory scan. Bypass terminology clarified. |
| 2.3.0 | Ninth review fixes: both traces produce pass-through steps (1:1 index correspondence). Iterative bypass-state fixed-point with terminal pre-pass. Visibility helper functions defined (build_regions, filter_to_cone, determine_obstruction_side, intersect_regions, see_through). Projective visibility source geometry (circle center for radial, parallel rays for directional). Origin-on-surface exclusion clarified. Frame ID sharing precondition. CycleOverride and cached_carriers formats defined. Checkpoint includes velocity. Surface coordinate frame stated. State change last-writer-wins. Planning multiple-carrier hit selection. |
| 2.4.0 | Tenth review fixes: cursor_index passed as parameter (not derived). compute_bypass_from_geometry defined with pseudocode. build_regions_from_casts pseudocode. Parallel-source visibility mode fully specified. Null-on-both-sides contradiction resolved (pass-through, not invisible). see_through added to intersection API (§11.6). Coincident surface tracing specified. Interactive pass-through prohibited. Iterative bypass convergence function defined. Plan display shows current effects. |
